server:
  port: 8081
  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 800
      min-spare: 30

spring:
  application:
    name: sszx-user
  profiles:
    active: dev

logging:
  level:
    com.zdf: debug
    org.springframework: warn

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml